const PurgomalumBaseService = require('./purgomalumBaseService.js');

module.exports = class PurgomalumContainsService extends PurgomalumBaseService {
    
    constructor(options) {
        super(options);
      }
}
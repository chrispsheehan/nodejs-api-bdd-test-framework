{
  "name": "nodejsapitestframework",
  "scripts": {
    "check": "tsc && cucumber-js features/ --dry-run",
    "test": "tsc && cucumber-js features/",
    "filter-test": "tsc && cucumber-js features/ --tags=\"$tags\""
  },
  "dependencies": {
    "chai": "^4.3.7",
    "cucumber": "^6.0.7",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/cucumber": "^7.0.0",
    "@types/supertest": "^2.0.12",
    "typescript": "^4.9.4"
  }
}